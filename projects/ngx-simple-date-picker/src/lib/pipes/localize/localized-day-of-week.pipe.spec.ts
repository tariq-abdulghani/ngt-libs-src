import { LocalizedDayOfWeekPipe } from './localized-day-of-week.pipe';

describe('LocalizedDayOfWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedDayOfWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
