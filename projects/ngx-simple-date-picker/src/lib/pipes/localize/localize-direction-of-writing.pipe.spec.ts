import { LocalizeDirectionOfWritingPipe } from './localize-direction-of-writing.pipe';

describe('LocalizeDirectionOfWritingPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizeDirectionOfWritingPipe();
    expect(pipe).toBeTruthy();
  });
});
